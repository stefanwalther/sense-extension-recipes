/*global define*/
define( [], function () {
	'use strict';

	return {
		checkboxDefault: {
			"type": "boolean",
			"ref": "props.chkbox.default",
			"label": "chk1) Checkbox, default values"
		}
	};

} );