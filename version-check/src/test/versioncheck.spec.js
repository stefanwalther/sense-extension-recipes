'use strict';

var mocha = require('mocha');
var should = mocha.should;

var versioncheck = require('./../components/swr-version-check/versioncheck.js');

describe( 'versioncheck', function () {

	it( '', function ( done ) {
		expect( true ).to.equal( false );
		done();
	} );
	
} );
